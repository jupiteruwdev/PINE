import { assert } from 'chai'
import { describe, it } from 'mocha'
import { SolBlockchain } from '../entities/lib/Blockchain'
import getSolNFTsByOwner from './getSolNFTsByOwner'

describe('core/getSolNFTsByOwner', () => {
  // it('can get all NFTs of an owner on Solana Mainnet', async () => {
  //   const blockchain = SolBlockchain()
  //   const ownerAddress = ''
  //   const nfts = await getSolNFTsByOwner({ blockchain, ownerAddress, populateMetadata: true })

  //   assert.isArray(nfts)
  // })
})
