version: "3.8"
services:
  app:
    command: npm start
    container_name: treasury-service
    environment:
      NODE_ENV: production
    image: ${IMAGE_NAME:-treasury-service}:${IMAGE_TAG:-latest}
    restart: always
    ports:
      - 8080:8080
